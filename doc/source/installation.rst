============
Installation
============

At the command line::

    $ pip install mercador-pub

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mercador-pub
    $ pip install mercador-pub
